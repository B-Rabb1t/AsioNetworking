{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build test",
            "type": "shell",
            "command": "g++ -g test.cpp -o test.out -pthread",
            "group": "build"
        },
        {
            "type": "cppbuild",
            "label": "C/C++: clang++-11 build active file",
            "command": "/usr/bin/clang++-11",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ]
}